# 6. 環境設定

[設定]→[環境設定]を選ぶと、8ページのプロパティシートが表示されます。

ページは、[編集1]、[編集2]、[ファイル]、[ウィンドウ]、[キーアサイン]、[ユーザーメニュー]、[ツールバー]、[ステータスバー]の順に並びます。

ページを切り替えると、入力内容がそのページの設定へ取り込まれます。

[OK]で全ページの変更を確定すると、設定が動作へ反映されて保存されます。

[キャンセル]で、環境設定を開く前の状態へ戻します。

表の初期値は、保存済みの設定がない状態で使われる値です。

保存済みの設定がある場合は、その値が初期表示になります。

以下の8ページに表示される設定は、現在の版で対応する動作へ反映されます。

UndoとRedoの退避データ上限はこのページには表示されず、環境設定画面から変更できません。

## 編集1

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [垂直移動スクロール行数] | 垂直スクロールバーの行単位で移動する行数です。入力範囲は1～999です。 | `1` |
| [上書モード時の貼り付けは上書き処理する] | 選択範囲がない上書きモードで、貼り付けを挿入ではなく上書きにします。 | オフ |
| [検索で見つからなかった場合にメッセージを表示する] | 検索結果がないとき、ビープ音の代わりにメッセージを表示します。 | オフ |
| [Escメニューを有効にする] | 選択範囲がないときの `Esc` を、[Esc メニュー]の2ストローク起動に使います。 | オフ |
| [Escキーで選択解除する] | 選択中に `Esc` を押したとき、選択を解除します。 | オフ |
| [選択データのコピー後に選択解除する] | [コピー]の後に選択範囲を解除します。 | オフ |
| [保存時にアンドゥバッファをクリアする] | 保存が成功したとき、元に戻す履歴とやり直し履歴を消去します。 | オフ |
| [サブキャレットを表示する] | アクティブでないデータ領域に、キャレット位置の下線を表示します。 | オン |
| [データ選択時にコード・文字共に反転表示する] | 選択範囲を16進領域と文字領域の両方で反転表示します。 | オン |
| [編集内容をリアルタイムでビットイメージに反映する] | 表示中のビットイメージを編集後に更新します。 | オン |
| [２ストロークキーのタイムアウト時間] | 2ストローク操作の第1打鍵を待つ時間を、0.0～2.0秒の0.1秒刻みで指定します。 | `1.5秒` |

### 編集時の補足

[上書モード時の貼り付けは上書き処理する]は、選択範囲がある貼り付けには適用されません。

選択範囲がある貼り付けは、常にその選択範囲を置き換えます。

[Escメニューを有効にする]を有効にすると、選択範囲がない状態の `Esc` が[Esc メニュー]の起動に使われます。

[２ストロークキーのタイムアウト時間]を0.0秒にしても、実際の待機時間は0.5秒になります。

設定値が0以下の場合に使われる待機時間です。両方のEsc設定を有効にした場合は、選択中には選択解除、選択範囲がないときには[Esc メニュー]の起動になります。

### UndoとRedoの上限

UndoとRedoの退避データ上限の初期値は256 MBです。

この上限は[編集1]ページの項目ではなく、現在の版では環境設定画面から変更できません。

上限を超えると、古い履歴から破棄されます。

移植版では、1回の編集で上限を超える量を退避する場合に、編集を続行して履歴を残さないか、編集を中止するかを確認するメッセージが表示されます。

## 編集2

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [ファイル履歴数] | [ファイル]メニューの[最新のファイル]に保持する履歴数です。入力範囲は0～16です。 | `5` |
| [キャレット位置の自動復元] | 前回閉じたファイルを再び開いたとき、保存されているキャレット位置を復元します。 | オフ |
| [現在位置を構造体編集アドレスに自動設定] | 構造体編集バーを表示したときや文書を切り替えたとき、キャレット位置を構造体の先頭アドレスにします。 | オン |
| [新規ドキュメントは常に編集可能として開く] | 新規文書を、拡張子別設定の編集禁止指定に関係なく編集可能で開きます。 | オン |
| [上書きモード時の末尾自動挿入] | 上書きモードでキャレットがデータ末尾にあるとき、入力を末尾へ追加できるようにします。 | オン |
| [ダイナミックマーク] | データの挿入・削除に合わせて、後ろのマークを移動します。削除範囲内のマークは削除します。 | オフ |

### ファイル履歴とキャレット位置

[ファイル履歴数]に0または1を入力した場合、実際の履歴作成時には5件として扱われます。

2～16の値は、その件数に合わせて履歴を再構築します。

### 構造体アドレスと編集モード

[現在位置を構造体編集アドレスに自動設定]がオンの場合、構造体編集バーの表示時と文書切り替え時に、現在のキャレット位置が基準になります。

オフの場合は、構造体編集バーに保持されているアドレスが使われます。

既存ファイルを開くときの編集禁止指定は、拡張子別設定の内容に従います。[新規ドキュメントは常に編集可能として開く]は、新規作成した文書に適用されます。

[上書きモード時の末尾自動挿入]をオフにすると、上書きモードでデータ末尾を越える入力は追加されません。

挿入モードでは、この設定に関係なく入力を追加できます。

[ダイナミックマーク]がオンの場合、挿入位置より後ろのマークは後ろへ移動し、削除位置より後ろのマークは前へ移動します。削除範囲内のマークは削除されます。

## ファイル

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [バックアップファイルの作成] | 既存ファイルを保存する前にバックアップを作成します。 | オン |
| [バックアップ世代数] | 保持するバックアップの世代数です。入力範囲は1～999です。 | `1` |
| [バックアップフォルダの指定] | 有効にすると、入力欄で指定したフォルダーへバックアップを作成します。 | オフ、フォルダー未指定 |
| [ファイルの排他制御] | [しない]、[書き込み禁止]、[読み書き禁止]から選びます。開いているファイルを他のプロセスからどこまで共有するかを決めます。 | [しない] |
| [リンクファイルは直接開く] | [開く]ダイアログで `.lnk` ファイルを選んだとき、リンク先ではなく `.lnk` ファイル自体を開きます。 | オフ |
| [デフォルトフォルダの指定] | [開く]ダイアログの初期フォルダーを指定します。 | オフ、フォルダー未指定 |

### バックアップ

[バックアップファイルの作成]がオンで、保存対象のファイルがすでに存在するときにバックアップが作成されます。

[バックアップ世代数]が1の場合は、保存対象のファイル名に `.bak` を付けたバックアップを作成します。

2以上では、古い世代を `.bk1`、`.bk2` のように繰り下げます。

[バックアップファイルの作成]をオフにすると、[バックアップ世代数]と[バックアップフォルダの指定]の入力欄が無効になります。

[バックアップフォルダの指定]をオフにした場合は、保存対象のファイルと同じフォルダーを使います。

### 排他制御と開くフォルダー

[ファイルの排他制御]の[書き込み禁止]では、他のプロセスの読み込みを許可し、書き込みを拒否します。

[読み書き禁止]では、他のプロセスの読み込みと書き込みの両方を拒否します。

ファイルを開いている間に設定されたロックを保持します。

ロックの取得に失敗した場合は、ロックを保持しない状態で開きます。

保存時は、書き込みのために一時的にロックを解放し、保存後に再びロックを取得します。

[バックアップフォルダの指定]と[デフォルトフォルダの指定]のチェックを外すと、それぞれのフォルダー入力欄と選択ボタンが無効になります。[リンクファイルは直接開く]をオフにすると、[開く]ダイアログの通常のリンク解決が使われます。

## ウィンドウ

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [メインウィンドウのサイズ・位置] | 起動時のメインウィンドウを[指定しない]、[前回終了時の位置・サイズ]、[最大化]、[指定]から選びます。[指定]では[左]、[上]、[幅]、[高さ]を入力します。 | [指定しない]。左0、上0、幅639、高さ479 |
| [ドキュメントウィンドウを最大化で開く] | 新しく開くドキュメントウィンドウを最大化します。 | オフ |
| [ドキュメントのフルパス表示] | ドキュメントウィンドウのタイトルにファイルのフルパスを表示します。 | オフ |
| [ツールバーの表示] | メインウィンドウのツールバーを表示します。 | オン |
| [ステータスバーの表示] | メインウィンドウのステータスバーを表示します。 | オン |
| [ビットイメージをドッキング可能にする] | ビットイメージバーを左右のドッキング領域へ移動できるようにします。 | オフ |
| [構造体編集バーの位置] | 構造体編集バーを[下]、[上]、[フローティング]のどこに配置するかを選びます。 | [フローティング] |
| [ドッキング不能とする] | 構造体編集バーがフローティングのとき、ドッキング操作を禁止します。フローティング以外では無効です。 | オン |
| [構造体編集バーのステータス表示] | 構造体編集バー内のステータスを[下]、[上]、[非表示]から選びます。 | [非表示] |
| [構造体アイテム幅の比率を保持] | 構造体編集バーの幅を変更したとき、[型]、[シンボル名]、[値]の列幅比率を保持します。 | オフ |

### メインウィンドウの配置

[前回終了時の位置・サイズ]を選ぶと、終了時の通常表示用の位置とサイズが次回起動時に使われます。

[指定]を選ぶと、[左]、[上]、[幅]、[高さ]の入力値が使われます。

### バーの表示と配置

[ツールバーの表示]と[ステータスバーの表示]は、[OK]で環境設定を確定したときに表示状態へ反映されます。

[構造体編集バーの位置]で[フローティング]を選んだときだけ、[ドッキング不能とする]を変更できます。

[構造体アイテム幅の比率を保持]をオンにすると、構造体編集バーの列幅を変更した後も、3列の比率が維持されます。

## キーアサイン

キーアサインでは、修飾キーとキーを選び、その組み合わせに機能を割り当てます。

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [キー] | 現在の修飾状態で選べるキーの一覧です。 | 修飾なしのキー一覧 |
| [Ctrl] | Ctrl修飾のキー一覧へ切り替えます。 | オフ |
| [Shift] | Shift修飾のキー一覧へ切り替えます。 | オフ |
| [機能] | 割り当てる機能のカテゴリと機能を選びます。 | [ファイル系]、未割り当て |
| [初期設定] | キー割り当てを既定値へ戻します。 | なし |
| [読み込み] | 256個のキー割り当てを `.key` ファイルから読み込みます。 | なし |
| [書き出し] | 256個のキー割り当てを `.key` ファイルへ書き出します。 | なし |

### 割り当てられる機能

機能は、[ファイル系]、[カーソル移動系]、[選択系]、[編集系]、[検索・置換系]、[メニュー系]、[ウィンドウ系]、[その他]の8カテゴリに分かれています。

機能一覧には、メニュー項目だけでなく、カーソル移動、選択、1バイト削除、入力モード切替、マーク操作なども含まれます。

現在の版では、次の112項目を割り当てられます。

| カテゴリ | 機能 |
| ---- | ---- |
| [ファイル系] | [新規作成]、[開く]、[閉じる]、[上書保存]、[名前を付けて保存]、[保存して閉じる]、[全て閉じる]、[全て保存]、[全て破棄]、[印刷]、[印刷プレビュー]、[プリンタの設定]、[全保存終了]、[全破棄終了]、[終了]、[ダンプイメージの保存]、[範囲を指定して印刷] |
| [カーソル移動系] | [カーソル左]、[カーソル右]、[カーソル上]、[カーソル下]、[データ先頭に移動]、[データ末尾に移動]、[行左端に移動]、[行右端に移動]、[高速上移動]、[高速下移動]、[ページアップ]、[ページダウン]、[半ページアップ]、[半ページダウン]、[ラインアップ]、[ラインダウン]、[指定アドレスへ移動]、[最終変更箇所へ移動] |
| [選択系] | [選択モード開始／終了]、[選択左]、[選択右]、[選択上]、[選択下]、[データ先頭まで選択]、[データ末尾まで選択]、[行左端まで選択]、[行右端まで選択]、[全て選択]、[前１ページ分選択]、[次１ページ分選択]、[範囲を指定して選択] |
| [編集系] | [元に戻す]、[やり直し]、[切り取り]、[コピー]、[貼り付け]、[編集禁止]、[上書／挿入切替]、[数値入力／文字入力切替]、[１バイト削除]、[直前の１バイト削除]、[選択範囲の削除]、[選択範囲の初期化]、[選択範囲をファイルに保存]、[編集前に戻す]、[構造体編集]、[キャレット位置を構造体編集] |
| [検索・置換系] | [検索]、[検索（上方向）]、[検索（モーダル）]、[検索（モーダル・上方向）]、[不一致検索]、[不一致検索（上方向）]、[不一致検索（モーダル）]、[不一致検索（モーダル・上方向）]、[次検索]、[前検索]、[置換]、[比較]、[BGREP]、[シンクロスクロール] |
| [メニュー系] | [ユーザーメニュー１]～[ユーザーメニュー１０]、[２ストローク機能１]～[２ストローク機能３] |
| [ウィンドウ系] | [新しいウィンドウを開く]、[重ねて表示]、[上下に並べて表示]、[左右に並べて表示]、[アイコンの整列]、[次のウィンドウ]、[ウィンドウ一覧]、[ウィンドウサイズ補正] |
| [その他] | [マーク登録／解除]、[次のマーク位置]、[前のマーク位置]、[全マークの解除]、[マーク一覧]、[名前を指定して実行]、[環境設定]、[拡張子別設定]、[キャラクターセット設定]、[ヘルプ]、[バイトオーダー設定]、[マーク２登録／解除]、[マーク３登録／解除] |

### キーの選択と反映

キー一覧は修飾状態によって変わります。

修飾なしではファンクションキー、[Home]、[End]、[Insert]などを選べます。

Shiftでは、矢印キーや[Delete]、[BackSpace]などが加わります。

CtrlまたはCtrl+Shiftでは、ファンクションキー、英字、記号、移動キーなどを選べます。

機能の変更は設定シート内の割り当てへ反映され、[OK]で環境設定を確定した後にキー入力へ反映されます。

### 既定の割り当て

次の表は、初期状態のキー割り当てのうち、メニュー項目に対応するものです。

キーアサインを変更していない場合の値です。

| キー | 機能 |
| ---- | ---- |
| `F1` | [ヘルプの表示] |
| `F3` | [次検索] |
| `Shift+F3` | [前検索] |
| `Ctrl+F4` | [閉じる] |
| `Ctrl+Home` | [データ先頭に移動] |
| `Ctrl+End` | [データ末尾に移動] |
| `Ctrl+F` | [検索] |
| `Ctrl+Z` | [元に戻す] |
| `Ctrl+Shift+Z` | [やり直し] |
| `Ctrl+X` | [切り取り] |
| `Ctrl+C`、`Ctrl+Insert` | [コピー] |
| `Ctrl+V`、`Shift+Insert` | [貼り付け] |

既定のキー割り当てには、メニューに表示されないカーソル移動、選択、入力モード切替、削除、次のウィンドウなども含まれます。

### キー割り当てファイル

[書き出し]では、256個の割り当てを2バイトずつ持つ `.key` ファイルを保存します。

[読み込み]では、同じ形式で長さが一致するファイルだけを読み込みます。読み込んだ割り当ては、[OK]で環境設定を確定すると使用されます。[初期設定]を押すと、確認後に既定の割り当てへ戻ります。

### ユーザーメニューと2ストローク機能

[メニュー系]には、設定済みのユーザーメニューを呼び出す[ユーザーメニュー１]～[ユーザーメニュー１０]があります。

[２ストローク機能１]～[２ストローク機能３]は、2ストローク操作を開始する機能です。

これらも通常の機能と同じ方法でキーへ割り当てられます。

2ストローク機能を割り当てたキーを押すと第1打鍵になり、設定したメニューのアクセラレータを第2打鍵として入力します。

タイムアウトすると、そのユーザーメニューが画面に表示されます。

## ユーザーメニュー

ユーザーメニューでは、ポップアップとして表示する機能の組み合わせを15個設定できます。

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [現在のメニュー設定] | [メニュー1]～[メニュー10]、[２ストローク機能1]～[２ストローク機能3]、[Esc メニュー]、[コンテキストメニュー]から編集対象を選びます。 | [メニュー1] |
| [追加できる機能] | カテゴリを選び、現在のメニューへ追加できる機能を表示します。 | [ファイル系] |
| [<<追加] | 選択した機能を現在のメニューへ追加します。追加時に[アクセラレータの指定]が表示されます。 | なし |
| [<<セパレータ] | 区切り線を現在のメニューへ追加します。 | なし |
| [削除>>] | 現在のメニューから選択した機能または区切り線を削除します。 | なし |
| [上へ移動] | 選択項目を1つ上へ移動します。 | なし |
| [下へ移動] | 選択項目を1つ下へ移動します。 | なし |

### 初期構成

初期状態では、[メニュー1]～[メニュー10]、[２ストローク機能1]～[２ストローク機能3]、[Esc メニュー]は空です。

[コンテキストメニュー]には、[切り取り]、[コピー]、[貼り付け]、区切り線、[選択範囲をファイルに保存]、[選択範囲の削除]、[選択範囲の初期化]が登録されています。

この[コンテキストメニュー]の登録内容は、データ表示領域の右クリックで表示されるメニューへ使われます。

[Esc メニュー]の内容は、[編集1]の[Escメニューを有効にする]がオンで、選択範囲がない状態の `Esc` で表示されます。

### 追加と並べ替え

機能は[ファイル系]など8カテゴリから選び、現在のメニューに追加済みの機能は候補から除外されます。

[<<追加]を押すと、1文字のアクセラレータを指定する[アクセラレータの指定]が表示されます。

アクセラレータは1文字で入力し、空欄のままでは[OK]を押せません。

現在の項目をダブルクリックすると、アクセラレータを変更できます。

アクセラレータの重複チェックはありません。

区切り線にはアクセラレータを設定できません。

[<<セパレータ]は、選択項目の直後、または現在のメニューの末尾に区切り線を追加します。

## ツールバー

ツールバーでは、表示するボタンと順序を設定します。

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [現在のツールバー設定] | 現在のボタン列を表示します。 | 既定のボタン列 |
| [追加できる機能] | カテゴリを選び、現在のボタン列に追加できる機能を表示します。 | [ファイル系] |
| [<<追加] | 選択した機能を現在のボタン列へ追加します。 | なし |
| [<<セパレータ] | 区切りを現在のボタン列へ追加します。 | なし |
| [削除>>] | 選択したボタンまたは区切りを削除します。 | なし |
| [上へ移動] | 選択したボタンを1つ上へ移動します。 | なし |
| [下へ移動] | 選択したボタンを1つ下へ移動します。 | なし |

### 初期のボタン列

初期状態のボタン列は、次の順序です。

- [新規作成]、[開く]、[上書保存]、区切り
- [元に戻す]、[やり直し]、区切り
- [切り取り]、[コピー]、[貼り付け]、[編集禁止]、[構造体編集]、区切り
- [検索]、[不一致検索]、[次検索]、[前検索]、[置換]、[比較]、[BGREP]、区切り
- [データ先頭に移動]、[データ末尾に移動]、[指定アドレスへ移動]、区切り
- [マーク登録／解除]、[次のマーク位置]、[前のマーク位置]、区切り
- [印刷]、[ヘルプ]

### 追加と並べ替え

候補は、[ファイル系]、[カーソル移動系]、[選択系]、[編集系]、[検索・置換系]、[ウィンドウ系]、[その他]に分類されています。[メニュー系]にはツールバー候補がありません。

現在のボタン列に追加済みの機能は、[追加できる機能]の候補から除外されます。

[<<追加]は候補を選んだときに使えます。[<<セパレータ]は、選択ボタンの直後、またはボタン列の末尾に区切りを追加します。

[削除>>]は現在のボタン列で項目を選んだときに使えます。[上へ移動]は先頭以外、[下へ移動]は末尾以外のボタンを選んだときに使えます。

[OK]で環境設定を確定すると、現在のボタン列からツールバーが再構築されます。

定義されていない機能はボタンとして作成されず、区切りとして扱われます。

## ステータスバー

ステータスバーでは、メッセージ領域の右側に表示する情報ペインの種類と順序を設定します。

| 項目 | 説明 | 初期値 |
| ---- | ---- | ---- |
| [現在のステータスバー設定] | 現在表示する情報ペインを順番に表示します。 | 既定の情報ペイン列 |
| [追加できる項目] | まだ設定に含まれていない情報ペインを表示します。 | [Caps Lock 表示]など |
| [<<追加] | 選択した情報ペインを現在の設定へ追加します。 | なし |
| [削除>>] | 選択した情報ペインを削除します。 | なし |
| [上へ移動] | 選択した情報ペインを1つ上へ移動します。 | なし |
| [下へ移動] | 選択した情報ペインを1つ下へ移動します。 | なし |

### 初期の情報ペイン

初期状態では、メッセージ領域に加えて、次の順序で表示されます。

- [アドレス表示（16進）]
- [変更有表示]
- [編集禁止表示]
- [挿入／上書表示]
- [ドキュメントサイズ（10進）表示]
- [キャラクターセット表示]

メッセージ領域は常にステータスバーの先頭に置かれ、設定画面の一覧には表示されません。

### 追加できる項目

追加できる項目は次のとおりです。

- [Caps Lock 表示]、[Num Lock 表示]、[Scroll Lock 表示]
- [挿入／上書表示]、[編集禁止表示]、[変更有表示]
- [ドキュメントサイズ（10進）表示]、[ドキュメントサイズ（16進）表示]
- [アドレス表示（10進）]、[アドレス表示（16進）]
- [BYTE値表示（10進）]、[BYTE値表示（16進）]
- [WORD値表示（10進）]、[WORD値表示（16進）]
- [DWORD値表示（10進）]、[DWORD値表示（16進）]
- [float値表示]、[double値表示]、[キャラクターセット表示]、[バイトオーダー表示]

現在の設定に含まれる項目は、[追加できる項目]の候補から除外されます。

### 追加と並べ替え

[<<追加]は追加候補を選んだとき、[削除>>]は現在の情報ペインを選んだときに使えます。[上へ移動]は先頭以外、[下へ移動]は末尾以外の情報ペインを選んだときに使えます。

[OK]で確定すると、ステータスバーが現在の設定順に再構築されます。

各ペインの値は、アクティブな文書のキャレット位置、編集状態、データ、キャラクターセット、バイトオーダーなどから更新されます。
