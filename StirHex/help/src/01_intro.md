# 1. はじめに

## StirHex とは

StirHex は、ファイルの内容を 16 進数と文字で表示・編集する Windows 向けのバイナリエディタです。
ファイルの読み込み、保存、編集、検索、比較、印刷などの機能を備えています。

StirHex は、BinaryEditor Stirling Version 1.31 をリバースエンジニアリングにより解析し、
現行の開発環境向けに再構築した非公式の移植版です。原作者が公開またはサポートする製品では
ありません。原版の著作権は原作者であるＤＤＳ２氏に帰属します。

StirHex は非営利かつ無保証です。本ソフトウェアの利用によって生じた損害について、開発者および
原作者は責任を負いません。権利関係と免責事項は、リポジトリ直下の `NOTICE.md` を確認してください。

移植版では、原版とすべての動作が一致することを保証しません。確認されている差異は
[8. 原版との差異](08_differences.html) にまとめています。

## 動作環境

StirHex を動かすために必要な環境は次のとおりです。

| 項目 | 内容 |
| ---- | ---- |
| OS | Windows 向けの GUI アプリケーションです。 |
| アーキテクチャ | 64 bit（`x64`）版と 32 bit（`Win32`）版があります。 |
| 文字セット | Unicode です。 |
| ランタイム | MFC と C/C++ ランタイムを静的リンクしています。Visual C++ ランタイムや MFC の追加インストールは不要です。 |

## 同梱ファイル

配布物の実行フォルダーには、次のファイルとフォルダーを置きます。

| ファイル名 | 説明 |
| ---- | ---- |
| `StirHex.exe` | StirHex 本体です。 |
| `help\` | ヘルプフォルダーです。`index.html` と各章の HTML を入れます。 |

`Struct.def` は同梱物ではなく利用者が用意するファイルです。`StirHex.exe` と同じフォルダーに置くと構造体編集で読み込まれます。
置かない場合は定義が読み込まれないだけで、エラーにはなりません。書式は第 5 章で扱います。

## インストールとアンインストール

インストーラはありません。`StirHex.exe` と `help` フォルダーを、実行する
場所へ手動で配置します。

ヘルプを表示するには、`help` フォルダーを `StirHex.exe` と同じフォルダーに置き、そこへ
`index.html` と各章の HTML を入れます。StirHex は実行ファイルのフォルダーから
`help\index.html` を組み立てて開きます。ファイルがない場合は、エラーメッセージが表示されます。

設定は INI ファイルや実行ファイルのフォルダーには保存されません。MFC のプロファイル API を
使い、次のレジストリキーへ保存されます。

`HKEY_CURRENT_USER\Software\StirHex\StirHex`

アプリ全体の設定はこのキーの `Env` セクションに保存されます。拡張子別の表示設定、キャレット
位置、ファイル履歴、実行履歴も同じキーの各セクションに保存されます。

設定は環境設定・拡張子別設定の確定時とアプリケーション終了時に保存されます。設定も削除する場合は、次の
手順を実行します。

1. StirHex を終了します。
2. `StirHex.exe` と `help` フォルダーを手動で削除します。
3. レジストリエディターで `HKEY_CURRENT_USER\Software\StirHex\StirHex` を削除します。

レジストリキーを削除すると、StirHex の設定、履歴、キャレット位置が削除されます。実行ファイル
と `help` フォルダーだけを削除した場合、レジストリに保存された設定は残ります。
