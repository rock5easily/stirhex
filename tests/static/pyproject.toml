[project]
name = "stirhex-static-tests"
version = "0"
description = "Static checks that read repository files without starting the application"
requires-python = ">=3.10"
dependencies = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["."]
addopts = "-v"
